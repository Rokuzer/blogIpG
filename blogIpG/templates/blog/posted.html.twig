{% extends 'blog/base.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-12 m-2">
                 <a href={{ path('blog_list')}} class="btn  btn-outline-dark m-2 float-end">Back to List</a>
            </div>
        </div>
        {% if post is defined %}
            <div class="row">
                {% if post.blog is defined %}
                    <div class="col-8 text-center">
                        <h1>{{post.blog.title}} </h1>
                        <p>{{post.blog.body}} </p>

                    </div>
                {% endif %}
                {% if post.autor is defined %}
                    <div class="col-4">
                        <div class="card">
                            <div class="card-header">
                                <h4>Autor data sheet</h4>
                            </div>
                            <div class="card-body">
                                <blockquote class="blockquote mb-0">
                                
                                    <p><strong>Name:</strong> {{post.autor.name}}</p>
                                    <p><strong>Username:</strong> {{post.autor.username}}</p>
                                    <p><strong>Email:</strong> {{post.autor.email}}</p>
                                    <p><strong>Address:</strong></p>
                                    <div class="card">
                                        <ul class="list-group list-group-flush">
                                        {% for key, dat in post.autor.address %}
                                            <li class="list-group-item"><strong>{{key|capitalize }}:</strong> {{dat | join(', ')}}</li>
                                        {% endfor %}
                                        </ul>
                                    </div>
                                    <p><strong>Phone:</strong> {{post.autor.phone}}</p>
                                    <p><strong>Website:</strong> {{post.autor.website}}</p>
                                    <p><strong>Company:</strong> </p>
                                    <div class="card">
                                        <ul class="list-group list-group-flush">
                                        {% for key, dat in post.autor.company %}
                                            <li class="list-group-item"><strong>{{key|capitalize }}:</strong> {{dat | join(', ')}}</li>
                                        {% endfor %}
                                        </ul>
                                    </div>
                                </blockquote>
                            </div>
                        </div>
                    </div>
                {% endif %}
            </div>

        {% endif %}

       
    </div>
{% endblock %}